# 从 environment.yaml 转换的 pip requirements.txt
# 用于 Kaggle 或其他仅支持 pip 的环境
# 注意：某些系统库（如 cudatoolkit, lib* 等）无法通过 pip 安装

# ===== 核心依赖（从 pip 部分） =====
nnaudio==0.2.4
timm==0.4.12
einops==0.3.2
colorednoise==1.1.1
complexpytorch==0.3
julius==0.2.5
torchsummary==1.5.1
kaggle==1.5.10

# ===== 基础科学计算 =====
# 注意：numpy 1.19.1 在某些环境中无法构建，已更新到 1.19.5（最小兼容版本）
# 注意：scipy 1.4.1 在某些环境中无法构建，已更新到 1.5.4（最小兼容版本）
numpy==1.19.5
scipy==1.5.4
pandas==1.2.3
scikit-learn==0.23.2
scikit-image==0.16.2

# ===== PyTorch 相关 =====
# 注意：Kaggle 通常已预装 PyTorch，但如需特定版本可取消注释
# torch==1.9.0
# torchvision==0.2.2
# torchaudio==0.9.0

# ===== 深度学习工具 =====
transformers==4.6.1
huggingface-hub==0.0.10
tensorboard==2.2.1
tensorboard-plugin-wit==1.6.0

# ===== 图像处理 =====
opencv-python==3.4.2
pillow==7.1.2
albumentations==0.4.6
imgaug==0.4.0
imageio==2.9.0

# ===== 音频处理 =====
librosa==0.8.1
audioread==2.1.9
soundfile==0.10.3.post1
resampy==0.2.2
pywavelets==1.1.1

# ===== 数据处理 =====
h5py==2.8.0
pyarrow==1.0.1
openpyxl==3.0.7

# ===== 可视化 =====
matplotlib==3.3.1
seaborn==0.11.1

# ===== 工具库 =====
tqdm==4.50.2
joblib==0.17.0
numba==0.51.2
llvmlite==0.34.0

# ===== 机器学习扩展 =====
lightgbm==2.3.0
catboost==0.24

# ===== HTTP 和网络 =====
requests==2.24.0
urllib3==1.25.10
certifi==2021.5.30

# ===== 配置和序列化 =====
pyyaml==5.3.1
toml==0.10.1

# ===== protobuf（注意版本兼容性，解决 MessageFactory 错误） =====
protobuf==3.13.0
grpcio==1.31.0

# ===== 其他依赖 =====
click==7.1.2
python-dateutil==2.8.1
pytz==2020.1
packaging==20.4
setuptools==50.3.0
wheel==0.35.1

# ===== 依赖库 =====
six==1.15.0
attrs==20.2.0
decorator==4.4.2
cachetools==4.1.1
filelock==3.0.12
fsspec==2021.7.0
typing-extensions==3.7.4.3

# ===== 代码质量工具（可选） =====
# autopep8==1.5.4
# pylint==2.7.4
# isort==5.8.0

# ===== 日本语处理（如果不需要可删除） =====
# fugashi==1.1.0
# ipadic==1.0.0
# mecab-python3==1.0.3
# mojimoji==0.0.11
# unidic==1.0.3
# japanize-matplotlib==1.1.3

# ===== 注意 =====
# 1. 如果遇到 protobuf/tensorboard 兼容性问题，尝试：
#    pip install "protobuf<4.22" tensorboard==2.14.0
# 
# 2. 如果遇到 timm 导入错误，确保版本为 0.4.12：
#    pip install timm==0.4.12
#
# 3. Kaggle 环境通常已预装 PyTorch，无需重复安装
#
# 4. 某些包可能需要系统级依赖（如 libsndfile 用于 soundfile）
#
# 5. 如果遇到 numpy 构建错误，已更新到 1.19.5（从 1.19.1）以修复构建问题
# 6. 如果遇到 scipy 构建错误，已更新到 1.5.4（从 1.4.1）以修复构建问题
